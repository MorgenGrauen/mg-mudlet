-- Hierin sammel ich alles, was für die GUI von Wichtigkeit sein könnte

--[[
Um eine Funktion durch ein Event auslösen zu lassen, gibt es zwei Möglichkeiten:
- Alle gewünschten Events in der GUI eintragen und auf Plus drücken. Dann muss aber 
  der "Script name" (bspw. "initGMCP" muss dem Namen einer Funktion in diesem Script
  entsprechen, sonst tut das nicht), oder
- Selbst im Code per registerAnonymousEventHandler() die Verbindung herstellen.
]]--
